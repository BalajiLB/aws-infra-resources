name: multi env deployment

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  deploy-dev:
    name: Deploy to Dev
    uses: ./.github/workflows/terraform-deploy.yml
    with:
      environment: dev

  deploy-stg:
    name: Deploy to Staging
    uses: ./.github/workflows/terraform-deploy.yml
    with:
      environment: stg

  deploy-prod:
    name: Deploy to Prod
    uses: ./.github/workflows/terraform-deploy.yml
    with:
      environment: prod
